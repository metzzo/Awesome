define([ ], function() {
  var Generator = function(mainNode) {
    this.mainNode = mainNode;
  };
  
  Generator.prototype.generate = function() {
    
  };
  
  return {
    Generator: Generator
  }
});